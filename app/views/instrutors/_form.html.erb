<%= form_for(instrutor, :html => {:class => "form-horizontal center"}) do |f| %>
  <% if instrutor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instrutor.errors.count, "error") %> prohibited this instrutor from being saved:</h2>

      <ul>
      <% instrutor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nome, class: "col-md-4 control-label" %>
    <div class="col-md-8">
    <%= f.text_field :nome %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :email, class: "col-md-4 control-label" %>
    <div class="col-md-8">
    <%= f.text_field :email %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :profissao, class: "col-md-4 control-label" %>
    <div class="col-md-8">
    <%= f.text_field :profissao %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-md-12">
      <%= f.submit 'Save', class: "btn btn-default btn-primary" %>
    </div>
  </div>
  
<% end %>
